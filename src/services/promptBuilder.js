const store = require("./store");

const SALES_POWER_TEXT = {
  light: "سجّل الطلب واجاوب على أسئلة الزبون بس، بلا ما تقترح إضافات.",
  mid: "اقترح إضافة أو عرض بس لو الفرصة مناسبة طبيعياً بالمحادثة، بدون إلحاح.",
  strong: "حاول تزيد قيمة كل طلب — اقترح إضافات، تكبير حجم، أو عروض العائلة، بأسلوب ودود مو مزعج.",
};

// مهارات بيع أساسية — تنطبق دايماً بغض النظر عن مستوى "قوة البيع" المختار، لأنها فرق بين موظف كفو وموظف عادي
const SALES_SKILLS_TEXT = `
- افهم شو الزبون محتاجه فعلاً قبل ما تقترح أي شي (كم شخص، جوعان قديش، بيفضل نوع معيّن) بدل ما تفترض.
- لو الزبون متردد أو مش عارف شو يطلب، اقترحله صنف أو صنفين بس (الأنسب لظرفه من المميزين بالمنيو) بدل ما تسرد له المنيو كامل.
- لو حسّ اعتراض على السعر، تعامل معه بثقة وهدوء — ركّز على القيمة (الكمية، الطازة، الحجم) بدل ما تعتذر أو توحي إنك ممكن تخفض السعر (ما عندك صلاحية تخفيض أسعار إطلاقاً).
- أول ما الزبون يقرر يطلب، اقفل الطلب وأكده فوراً بدل ما تفتح موضوع جديد ممكن يخليه يتردد أو يغيّر رأيه.
- خلي كل رد يقرب المحادثة خطوة من تأكيد الطلب: إما سؤال يوضح تفصيلة ناقصة، أو اقتراح مناسب، أو تأكيد نهائي — مش حكي عام بلا هدف.
- لو الزبون بعتلك رسالة وحدة فيها أكتر من معلومة يلي طلبتها منه (مثلاً اسمه وعنوانه ورقمه مع بعض)، اعتبرها إجابة كاملة على كل أسئلتك — لخّصها له وأكد الطلب مباشرة، أو اسأل بس عن أي تفصيلة ناقصة فعلاً. ما ترجع تسأله سؤال عام كأنك ما استلمت ردّه، وما تعيد نفس السؤال يلي أجاب عليه لتوّه.
- تجنّب عبارات المتابعة العامة والمكررة زي "يلا في حاجة؟" أو "بدك تطلبي ولا لسه مترددة؟" — هاي بتحس الزبون إنك بتلاحقه بشكل آلي. لو حابب تتابع مع زبون ساكت، كون محدد ومحترف (اسأله عن تفصيلة معينة ناقصة، أو ذكّره بعرض محدد) بدل سؤال عام مفتوح.`;

const GUARDRAIL_TEXT = {
  "no-prices": "ما تخترع أسعار مش موجودة بقائمة المنيو.",
  "no-competitors": "ما تذكر أي مطعم أو منافس إطلاقاً.",
  neutral: "ابقى محايد إذا انجرّت المحادثة لمواضيع حساسة أو مثيرة للجدل.",
  "no-medical": "ما تقدم نصائح شخصية أو طبية أو قانونية.",
  "no-fake-promise": "ما تعطي وعود كاذبة عن مواعيد توصيل أو اتصالات.",
  privacy: "احمِ خصوصية بيانات الزبون، وما تشاركها مع أي طرف.",
  "no-repeat": "تجنب تكرار نفس الرد بشكل يضايق الزبون.",
  "no-links": "ما تشارك روابط خارجية.",
  "stay-on-topic": "ابقى بموضوع الطلب، ولو الزبون حاد عنه رجّعه بلطف.",
  "no-inappropriate": "احظر أي محتوى غير لائق بالمحادثة.",
};

function isOpenNow(openTime, closeTime, nowJordan = new Date()) {
  // ساعات دوام تعدّي منتصف الليل (مثلاً 15:00 → 04:30)
  const [oh, om] = openTime.split(":").map(Number);
  const [ch, cm] = closeTime.split(":").map(Number);
  const minutesNow = nowJordan.getHours() * 60 + nowJordan.getMinutes();
  const openMinutes = oh * 60 + om;
  const closeMinutes = ch * 60 + cm;

  if (openMinutes > closeMinutes) {
    // بيعدّي منتصف الليل
    return minutesNow >= openMinutes || minutesNow < closeMinutes;
  }
  return minutesNow >= openMinutes && minutesNow < closeMinutes;
}

// نص صغير منفصل عن البرومبت الثابت (Mشان ما نكسر الـ prompt caching — لو حطينا بيانات الزبون جوا البرومبت
// الكبير المخزّن مؤقتاً، رح يصير كل زبون جديد يدفع سعر كامل بدل الخصم، لأن أي تغيير بالنص بيلغي الكاش).
// هاد النص منفصل، بينضاف كبلوك تاني بالـ system array بدون cache_control (شوف ai.js).
function buildCustomerProfileSection(customerProfile) {
  if (!customerProfile) return "";
  return `
معلومات زبون سابق (عندنا سجل له من طلب قبل هيك، نفس رقم التواصل):
- الاسم: ${customerProfile.name || "غير مسجل"}
- رقم التواصل: ${customerProfile.phone || "غير مسجل"}
- آخر عنوان توصيل: ${customerProfile.area || "غير مسجل"}
- آخر أصناف طلبها: ${(customerProfile.lastItems || []).join("، ") || "غير مسجلة"}
استخدم هاي المعلومات: رحّب فيه بشكل طبيعي كزبون سابق، واسأله بلطف إذا بده يوصل لنفس العنوان يلي فوق قبل ما تطلب منه يعيد يعطيك عنوانه من الصفر. لو قالك تغيّر شي (عنوان، رقم)، اعتمد الجديد. لو في "معلومات زبون سابق" هون، اعتمدها بدل ما تسأل الزبون من الصفر عن اسمه/رقمه/عنوانه — بس تأكد منه إذا لسا نفس التفاصيل قبل ما تأكد الطلب النهائي.`.trim();
}

// خصم الزبون (VIP من الكاشير برقم هاتفه، أو كود خصم كتبه بنفس الرسالة) — منفصل عن البرومبت الثابت لنفس سبب
// بلوك بيانات الزبون فوق (ما نكسر الـcache)، وكمان عشان قائمة الأكواد نفسها ما تنكشف أبداً لموديل الذكاء
// الاصطناعي — هو بس بيشوف نتيجة الكود يلي الزبون كتبه فعلاً هالرسالة، مش كل الأكواد الموجودة بالنظام.
function buildDiscountSection(discountContext) {
  if (!discountContext) return "";
  const lines = ["خصم الزبون (طبّقه فوراً بدون ما تحتاج تسأل، واذكره للزبون بوضوح بردك، واطرحه من المجموع بأداة record_order):"];

  if (discountContext.vip) {
    lines.push(`- هاد الزبون عميل مميز (VIP) بالنظام: خصم ${discountContext.vip.percent}% على كل طلبه.`);
  }
  if (discountContext.code) {
    const desc = discountContext.code.kind === "percent" ? `${discountContext.code.value}%` : `${discountContext.code.value} د.أ`;
    lines.push(`- الزبون كتب كود خصم "${discountContext.code.code}" وهو صحيح وفعّال: خصم ${desc}.`);
  }

  return lines.join("\n");
}

// قائمة الفروع (بدون سقف عدد — البوت بيدعم فرع واحد أو أكتر بنفس الطريقة) — بتظهر بالبرومبت عشان البوت
// يعرف يجاوب عن عناوين الاستلام وساعات كل فرع لحاله، ويستخدمها كمان لحساب أقرب فرع لموقع الزبون (deliveryCalc.js).
function buildBranchesSection(branches, nowJordan = new Date()) {
  if (!Array.isArray(branches) || !branches.length) return "";
  const lines = branches.map((b) => {
    const open = b.open24h ? true : b.openTime && b.closeTime ? isOpenNow(b.openTime, b.closeTime, nowJordan) : true;
    const hours = b.open24h ? "24 ساعة" : b.openTime && b.closeTime ? `${b.openTime} - ${b.closeTime}` : "غير محدد";
    const status = open ? "فاتح هلأ" : "مسكر هلأ";
    return `- ${b.name}: ${b.address || "بدون عنوان مسجل"}${b.mapLink ? ` (رابط خرائط: ${b.mapLink})` : ""} — الدوام: ${hours} (${status})`;
  });
  return `فروع ${branches.length > 1 ? "المحل المتاحة" : "المحل"} (للاستلام أو كمرجع أقرب فرع للتوصيل):\n${lines.join("\n")}`;
}

// قائمة المناطق برسوم ثابتة (مقسّمة حسب الفرع لو كل منطقة مربوطة بفرع معيّن) — قبل هيك كان البرومبت
// بيذكر بس "عدد" المناطق بدون يسرد أسماءها وأسعارها الفعلية، يعني الذكاء الاصطناعي ما كان يقدر أبداً
// يجاوب بسعر صحيح لمنطقة مكتوبة بالنص (بس يقدر يحسب سعر دقيق من الموقع المباشر). هلأ منسرد القائمة كاملة.
function buildDeliveryAreasSection(fees, branches) {
  if (!Array.isArray(fees) || !fees.length) return "";

  const branchNameById = new Map((branches || []).map((b) => [b.id, b.name]));
  const groups = new Map(); // اسم المجموعة (فرع أو "عام") -> أسطر المناطق

  for (const f of fees) {
    if (!f?.area || typeof f.fee !== "number") continue;
    const label = f.branchId ? branchNameById.get(f.branchId) || "فرع غير مسجل" : "عام (كل الفروع)";
    if (!groups.has(label)) groups.set(label, []);
    groups.get(label).push(`- ${f.area}: ${f.fee} د.أ`);
  }

  if (!groups.size) return "";

  const sections = [...groups.entries()].map(([label, lines]) => `${label}:\n${lines.join("\n")}`);
  return `مناطق التوصيل المسجلة برسوم ثابتة (${fees.length} منطقة بالنظام) — استخدمها إذا الزبون كتب اسم منطقته بالنص بدل ما يبعت موقعه المباشر. طابق اسم المنطقة يلي كتبها الزبون مع القائمة تحت فقط لو نفس المنطقة بالضبط (أو نفس الاسم بصياغة مختلفة بسيطة) — ممنوع تستخدم سعر منطقة "قريبة" أو "مشابهة" لو مش هي نفسها فعلاً، وإذا ما لقيتها، اعتبرها غير مسجلة:\n\n${sections.join("\n\n")}`;
}

// موقع مباشر بعته الزبون (Live/Pin Location) — محسوب سلفاً بالكود (deliveryCalc.js)، مش بالذكاء الاصطناعي،
// عشان رسم التوصيل يكون دقيق دايماً بدل ما يتخمّن. منفصل عن البرومبت الثابت (بدون cache_control) لنفس سبب
// بلوكات الزبون/الخصم فوق — خاص بهالرسالة بس، وممكن يختلف كل مرة الزبون يبعت موقع جديد.
function buildLocationSection(locationContext) {
  if (!locationContext) return "";
  const { address, branch, distanceKm, fee, estimated } = locationContext;
  const lines = [
    "الزبون بعت موقعه المباشر (📍 Live Location) من واتساب/ماسنجر — اعتمد هاي المعلومات المحسوبة تلقائياً كمنطقة/عنوان توصيله، ولا تسأله يكتب منطقته من جديد:",
  ];
  if (address) lines.push(`- العنوان التقريبي حسب الموقع المُرسل: ${address}`);
  if (branch) {
    lines.push(
      `- أقرب فرع لموقعه: ${branch.name}${typeof distanceKm === "number" ? ` (${distanceKm} كم عن طريق الطريق الفعلي${estimated ? " — تقدير تقريبي" : ""})` : ""}`
    );
  }
  if (typeof fee === "number") {
    lines.push(`- رسم التوصيل المحسوب فعلياً لهالموقع: ${fee} د.أ — استخدم هالرقم بالضبط بمجموع الطلب، بدون أي تعديل أو تقريب إضافي منك.`);
  } else {
    lines.push(
      "- ما قدرنا نحسب رسم توصيل دقيق لهالموقع (بعيد جداً عن كل الفروع المسجلة أو صار خطأ بالحساب) — اعتذر للزبون بلطف وقوله التوصيل مش مؤكد إلها حالياً، واعرض الاستلام من الفرع أو خذ منه تفاصيل إضافية."
    );
  }
  return lines.join("\n");
}

function buildSystemPrompt(configId = "default") {
  const settings = store.read(`configs/${configId}/settings.json`);
  const menu = store.read(`configs/${configId}/menu.json`);
  const fees = store.read(`configs/${configId}/deliveryFees.json`);
  const branches = store.exists(`configs/${configId}/branches.json`) ? store.read(`configs/${configId}/branches.json`) : [];

  const { identity, style, guardrails, customRules, prompt } = settings;

  const open = identity.open24h ? true : isOpenNow(identity.openTime, identity.closeTime);

  const menuText = menu
    .map((item) => {
      const price = item.price !== null ? `${item.price} د.أ` : "السعر غير مسجل بعد بالنظام";
      const avail = item.available ? "متوفر" : "غير متوفر حالياً";
      const featured = item.featured ? ` — [صنف مميز: ${item.featuredNote || "يُنصح باقتراحه"}]` : "";
      return `- ${item.name} (${item.category}): ${price}، ${avail}${featured}`;
    })
    .join("\n");

  const guardrailText = Object.entries(guardrails)
    .filter(([, on]) => on)
    .map(([id]) => `- ${GUARDRAIL_TEXT[id] || id}`)
    .join("\n");

  const rulesText = customRules.map((r) => `- ${r}`).join("\n");

  const dialect = identity.dialect || "الأردن";
  // اسم عام نستخدمه بأي جملة عامة عن "المطعم" — لو البوت لشركة/محل مش مطعم (مثلاً منتجات تنحيف)، منستخدم اسمه الفعلي بدل "المطعم"
  const placeName = identity.businessName || "المطعم";

  // بنعلم الذكاء الاصطناعي إنو فيه إمكانية إرسال صور تلقائياً بس لو الميزة مفعّلة فعلاً وفي صنف واحد ع الأقل
  // عندو صورة محفوظة — وإلا بيوعد الزبون بصورة ما رح توصله (شوف findMentionedItemsWithImages بـ messageHandler.js).
  const hasMenuImages = Boolean(settings.sendImagesAutomatically) && menu.some((item) => item.imageUrl);
  const branchesText = buildBranchesSection(branches);
  const areasText = buildDeliveryAreasSection(fees, branches);

  return `
قواعد صارمة قبل أي شي (لو تعارضت مع أي تعليمة تانية بالبرومبت، هاي القواعد تفوز دايماً):
1. ما تجاوب ولا معلومة (صنف، سعر، توفر، عرض، منطقة توصيل) إلا إذا كانت موجودة حرفياً بقائمة المنيو أو رسوم التوصيل تحت. لو الزبون سأل عن شي مش موجود، قوله بصراحة إنه مش موجود بدل ما تختلق جواب.
2. لو السعر مسجل "غير مسجل بعد بالنظام"، قول للزبون إنك بتتأكد من السعر وترجعله، ولا تقدّر أو تخمّن أي رقم.
3. ما تأكد أي طلب أو تعتبره مُرسل قبل ما الزبون يأكد صراحة كل التفاصيل (الصنف، الكمية، المنطقة، رقم التواصل).
4. لو مش متأكد من معلومة، قول "خليني أتأكد" بدل ما تختلق إجابة تبدو مقنعة.
5. احكي عربي عامي بلهجة ${dialect} طبيعية، متل موظف حقيقي عم يحكي أول مرة — مش متل قالب جاهز. نوّع صياغة جملك دائماً: غيّر ترتيب الكلمات والتعابير والمقدمات من محادثة لمحادثة، حتى لو نفس المعنى أو نفس السؤال تكرر مع أكتر من زبون. ممنوع تستخدم نفس الجملة حرفياً مع أكتر من زبون.
6. أول ما الزبون يأكد صراحة كل تفاصيل طلبه (الأصناف والكمية، والمنطقة أو الاستلام)، استدعي أداة record_order بنفس الرد يلي بتأكد فيه الطلب للزبون — مرة وحدة بس لكل طلب. بدون ما تستدعيها، الطلب ما بينسجل ولا حدا بيشوفه، حتى لو قلت للزبون إنه انسجل.
7. قاعدة غير قابلة للنقاش: ممنوع تمنعاً باتاً تكتب للزبون أي جملة فيها معنى "تم تسجيل طلبك" أو "تم تأكيد طلبك" أو "طلبك انسجل بنجاح" أو أي صياغة مشابهة إلا إذا استدعيت أداة record_order بنفس هالرد بالضبط. الجملة والأداة لازم يصيروا مع بعض دائماً — ما تنفصلوا أبداً. لو حابب تأكد للزبون قبل ما تكون جاهز تستدعي الأداة، استخدم صياغة غير نهائية زي "تمام، خليني أسجللك الطلب" بدل "تم تسجيله".
${hasMenuImages ? `8. لو الزبون طلب صراحة يشوف صورة صنف، أو كان محتار وقررت تغريه بصنف معيّن، استدعي أداة send_photo (باسم الصنف بالضبط أو أقرب صيغة له) — النظام بيرسل الصورة تلقائياً بعد ردك. ما تستدعيها لمجرد ذكر اسم صنف بسياق عادي بردك (هيك بزعج الزبون بصور ما طلبها)، وما تقول أبداً "ما عندي إمكانية إرسال صور".` : ""}
9. لو الزبون قال إنو عنده كود خصم بس ما ظهرلك قسم "خصم الزبون" منفصل بالأسفل يأكده، اطلب منه يكتب الكود بالضبط برسالة لحاله (بدون كلام إضافي حواليه) عشان تقدر تتأكد منه. ما تفترض أبداً إنو الكود صحيح أو تطبق أي خصم من عندك — بس اعتمد على قسم "خصم الزبون" لو ظهر.
10. لو الزبون بعت موقعه المباشر (Live Location) من واتساب أو ماسنجر، رح يظهرلك قسم منفصل بالأسفل فيه العنوان التقريبي وأقرب فرع ورسم التوصيل المحسوب فعلياً لهالموقع — اعتمد هالرقم بالضبط ولا تسأل الزبون عن منطقته من جديد. لو ما ظهر هيك قسم، اسأل الزبون عن منطقته بالكتابة زي العادة أو اقترح عليه يبعت موقعه المباشر (📍) عشان تحسبله رسم توصيل أدق.
11. لو الزبون اعترض على رسم التوصيل، حسّه غالي، أو حس إنك "مستغله"/بتضحك عليه: ما تدافع بعصبية ولا تلوم الزبون. اشرحله بهدوء وثقة إنو الرسم مش رقم عشوائي — محسوب حسب المسافة الفعلية من أقرب فرع (أو حسب منطقة مسجلة بالنظام)، ونفس الرسم لكل الزبائن بنفس المسافة. ما عندك صلاحية تخفض رسم التوصيل من عندك إطلاقاً ولا تعطي وعد بمراجعته لاحقاً. لو لسا مش قانع، اعرض عليه بلطف بديل الاستلام من الفرع (بدون أي رسم توصيل) كخيار فعلي، وخليه يقرر.
12. لو ${placeName} مسكر هلأ وحب الزبون يأكد طلب لوقت الفتح، اقبل تسجيله كـ"حجز": أكد الأصناف معه وسجل بملاحظات record_order إنو التوصيل/الاستلام رح يصير بعد وقت الفتح. لو الزبون مستعجل أو الموضوع مش طلب عادي (شكوى، استفسار عاجل، مشكلة بطلب سابق)، أعطيه رقم الدعم/السنتر: ${identity.supportPhone || "غير مسجل بالنظام"}.
13. لما تستدعي أداة record_order، احرص تعبّي حقولها الجديدة صح: لو استلام من الفرع، حط اسم الفرع بالضبط بحقل branch (وخلي area فاضي). لو الزبون حدد طريقة تواصل مفضّلة (واتساب، اتصال، إلخ) حطها بحقل contactMethod. وفي subtotal حط مجموع الأصناف بس (بعد أي خصم)، وفي deliveryFee رسم التوصيل الفعلي (0 أو فاضي لو استلام) — لا تحط بس المجموع الكلي بحقل subtotal.

اسمك "${identity.employeeName}" — موظف بشري بـ${identity.businessName || "مطعم Saber Jo Snack"}${identity.businessAddress ? ` (${identity.businessAddress})` : " بعمّان (فرع طلوع الحوانا، شارع الجامعة الأردنية)"}. ${identity.businessType || "توصيل واستلام بس، بدون تناول بالمكان"}.

أوقات الدوام: ${identity.open24h ? `${placeName} فاتح 24 ساعة طول الأسبوع.` : `من ${identity.openTime} لـ ${identity.closeTime}.`}
حالة ${placeName} هلأ: ${open ? "فاتح" : "مسكر"}.
${open ? "" : `إذا سأل الزبون عن الطلب هلأ، أخبره بلطف إنو ${placeName} مسكر هلأ ووضحله وقت الفتح.`}

نبرة الحكي: ${style.tones.join("، ")}. مستوى استخدام الإيموجي: ${style.emojiLevel}. حاول تخلي ردودك تحت ${style.responseLength} حرف تقريباً.

قوة البيع: ${SALES_POWER_TEXT[identity.salesPower]}
مهارات بيع لازم تطبّقها دايماً (أنت مندوب مبيعات شاطر، مش موظف بيرد بس):${SALES_SKILLS_TEXT}

قائمة المنيو الحالية (السعر والتوفر من النظام مباشرة، ما تخترع غيرها):
${menuText}

${areasText || "ولا منطقة توصيل مسجلة برسم ثابت بالنظام حالياً — اعتمد بس على الموقع المباشر (قاعدة 10) أو الاستلام من الفرع."} لو الزبون بعت موقعه المباشر، الرسم بينحسب تلقائياً بدقة أكتر حسب المسافة الفعلية (شوف القاعدة 10 فوق) وبتلاقيه بقسم منفصل بالأسفل. إذا المنطقة يلي كتبها الزبون مش مسجلة بالقائمة فوق ولا بعت موقعه، اعتذر وقول التوصيل مش مؤكد إلها واعرض الاستلام من الفرع، أو اقترح عليه يبعت موقعه المباشر (📍) نحسبله السعر بالضبط.
${branchesText ? `\n${branchesText}\n` : ""}
حواجز الأمان:
${guardrailText}

قواعد إضافية خاصة بـ${placeName}:
${rulesText}

${prompt}
`.trim();
}

module.exports = { buildSystemPrompt, buildCustomerProfileSection, buildDiscountSection, buildLocationSection, isOpenNow };
